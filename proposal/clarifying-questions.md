# Clarifying Questions for Meridian Procurement

1. For the Reports module defects, can you share a prioritized list of the eight issues already logged, or confirm whether the scope should include all report-related filters, i18n labels, and data consistency issues observed in the current page?
2. For Restocking recommendations, should the purchase order suggestions assume a fixed supplier lead time or should we allow operators to override lead time and safety stock assumptions through the UI?
3. Which user flows are considered "critical" for automated browser testing? Examples include: generating a report, applying filters, creating a restocking recommendation, and changing locale/theme.
4. Do you expect the architecture documentation to include deployment diagrams, component interactions, and data flow, or is a high-level application overview sufficient for the initial handoff?
5. For the desired items (UI modernization, i18n extension, dark mode), do you want us to include optional estimates as separate change requests, or should we fold the strongest one into the base proposal if it can be delivered within a single phase?